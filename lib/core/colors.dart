import 'package:flutter/material.dart';

class DarkwoodColors {
  DarkwoodColors._();

  // Core
  static const Color background = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);
  static const Color accent = Color(0xFFFCC44C);
  static const Color paleAccent = Color(0xFFFFF8ED);

  // Supporting — surfaces
  static const Color surface = Color(0xFFF5F5F5);
  static const Color surfaceElevated = Color(0xFFEDEDED);

  // Supporting — accent shades
  static const Color accentDark = Color(0xFFE0AA20);
  static const Color accentLight = Color(0xFFFDD97A);

  // Supporting — text
  static const Color textPrimary = Color(0xFF000000);
  static const Color textSecondary = Color(0xFF4A4A4A);
  static const Color textMuted = Color(0xFF9A9A9A);

  // Supporting — utility
  static const Color divider = Color(0xFFEAEAEA);
  static const Color error = Color(0xFFE0453A);
  static const Color success = Color(0xFF3DAA6F);
  static const Color overlay = Color(0x80000000);
}
